todo: 

For evals, I currently label melted key:value pairs. I.e a single ground truth is $13.7 but we don't have the key bit of info tied to that i.e. period. We need a way to combine it all. That way we can eval the prediction vs. ground truth. 